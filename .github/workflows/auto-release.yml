name: Auto Release Patch

on:
  pull_request:
    branches: [main]
    types: [closed]

permissions:
  contents: write

jobs:
  auto-release-patch:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}

      - name: Get next version
        id: version
        run: |
          git fetch --tags
          LATEST=$(git tag --sort=-v:refname | grep -E '^v[0-9]+\.[0-9]+\.[0-9]+$' | head -1)
          LATEST="${LATEST:-v0.0.0}"
          echo "current=${LATEST}" >> $GITHUB_OUTPUT

          MAJOR=$(echo "$LATEST" | sed 's/v//' | cut -d. -f1)
          MINOR=$(echo "$LATEST" | sed 's/v//' | cut -d. -f2)
          PATCH=$(echo "$LATEST" | sed 's/v//' | cut -d. -f3)
          NEXT="v${MAJOR}.${MINOR}.$((PATCH + 1))"
          echo "next=${NEXT}" >> $GITHUB_OUTPUT

      # Embed an up-to-date changelog in the release tag so the app reports the
      # correct version (it reads the embedded changelog). main is PR-protected
      # by a ruleset, so we commit this onto the tag only — never push to main.
      - name: Create and push tag
        run: |
          if git rev-parse "${{ steps.version.outputs.next }}" >/dev/null 2>&1; then
            echo "Tag ${{ steps.version.outputs.next }} already exists, skipping"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          python3 scripts/sync_changelog.py "${{ steps.version.outputs.next }}"
          if ! git diff --quiet changelog.json; then
            git add changelog.json
            git commit -m "chore(changelog): ${{ steps.version.outputs.next }}"
          fi
          git tag "${{ steps.version.outputs.next }}"
          git push origin "${{ steps.version.outputs.next }}"

      - name: Summary
        run: |
          echo "### Auto Release" >> $GITHUB_STEP_SUMMARY
          echo "- Previous: \`${{ steps.version.outputs.current }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Released: \`${{ steps.version.outputs.next }}\`" >> $GITHUB_STEP_SUMMARY
